# AS Hack Club Tracker

An app to help 'Ashford School Hack Club' track members workshops, submitted items, attendance, food allergies etc.

## To Start Server on Nest

Run:

`cd ~/club-tracker`
`git pull`
`python3 -m venv venv`
`source venv/bin/activate`
`python3 app.py`

The server is now activate!